# =============================================================================
# Sentinel-KE — Runtime dependencies
# Installed in every backend container (API, workers, consumer).
# =============================================================================

# Web framework
fastapi
uvicorn[standard]
gunicorn
starlette
python-multipart

# Data validation
pydantic
email-validator

# ORM & database
sqlalchemy
psycopg2-binary
alembic

# Graph database
neo4j

# Search & analytics
opensearch-py

# Streaming (Kafka / Redpanda)
kafka-python

# HTTP & cloud
requests
httpx
boto3

# STIX threat intelligence
stix2

# Rate limiting
slowapi

# Post-quantum cryptography (NIST FIPS 203 / 204 / 197)
# AES-256-GCM + HKDF-SHA3-512 — replaces custom XOR cipher for MFA secret storage
cryptography
# ML-KEM-768 (CRYSTALS-Kyber) — FIPS 203 key encapsulation
kyber-py
# ML-DSA-65 (CRYSTALS-Dilithium) — FIPS 204 digital signatures for tokens
dilithium-py
# AES-CTR DRBG backend required by kyber-py and dilithium-py for deterministic keygen
pycryptodome
