# =============================================================================
# Sentinel-KE — ML / GPU dependencies
# Installed in notebook container and backend when INSTALL_ML=1.
#
# GPU upgrade (for hackathon T4 CUDA 12.1):
#   Change index URL  →  https://download.pytorch.org/whl/cu121
#   Change package    →  torch (no +cpu suffix needed for CUDA build)
# =============================================================================

# CPU build (local dev default)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
