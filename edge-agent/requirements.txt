# Sentinel-KE Edge Agent — Python dependencies
# Core runtime (always required)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
pytest

# PyTorch GNN stack (remove if running heuristic-only / lightweight mode)
torch>=2.2.0
torch-geometric>=2.5.0

# Optional: CSV connector
# (no extra dep — uses stdlib csv)

# DbConnector — SQLAlchemy core (required for any db data_source)
sqlalchemy>=2.0

# DbConnector — install the driver(s) matching your database:
#   PostgreSQL  →  psycopg2-binary
#   MySQL       →  pymysql
#   MSSQL       →  pyodbc
#   Oracle      →  cx_oracle
#
# Uncomment the line(s) you need:
# psycopg2-binary>=2.9   # PostgreSQL (KRA iTax, IFMIS, KEMSA, M-Pesa)
# pymysql>=1.1           # MySQL / MariaDB
# pyodbc>=5.0            # Microsoft SQL Server (MSSQL)
